import { Grid, Typography } from '@mui/material'
import React from 'react'

const Header = () => {
  return (
    <Grid>
      <Typography variant='h3' sx={{marginTop:2}}>Task Manager</Typography>
    </Grid>
  )
}

export default Header
